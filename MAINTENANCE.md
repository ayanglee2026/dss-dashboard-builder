# 维护手册（MAINTENANCE）

沉淀类 skill 放久了会跟实际脱节。本文件定义如何维护、何时更新、怎么判断过期。**半年后回来，先读这份再决定要不要改。**

## 版本号规则

`主.次` 两位：

| 变更类型 | 版本位 | 例子 |
|---------|-------|------|
| 结构性改工作流、加/删阶段、改核心原则 | 主版本 | 工作流 6 阶段 → 7 阶段 |
| 加 reference、改示例、去业务化、小修 | 次版本 | v1.1（去业务化）→ v1.2（MySQL + 维护机制） |

## 更新触发条件

以下任一发生，就该更新：

1. **源项目换了技术栈 / 踩了新坑**——新增的坑要沉淀成避坑或 reference
2. **发现新的通用领域**——如新增医疗、餐饮领域映射，更新 `domain-mapping.md`
3. **某条避坑失效**——如 CDN 缓存机制变了、某个库升级了行为变了
4. **references 之间耦合断裂**——AI 按需只读一篇时拼不出完整方案
5. **使用者在真实项目里发现失准**——记进"待校准"清单（见下）

## 更新流程

```
改 skill → 改对应 reference → 更新版本号 → CHANGELOG 记一笔 → 同步所有安装位置
```

同步位置：
- Claude Code：`~/.claude/skills/`
- WorkBuddy：`~/.workbuddy/skills/`
- Cursor：项目 `.cursor/rules/`（需转换格式）

每次更新**重打包分享包**，发给使用者（版本号 + CHANGELOG 一起更新）。

## 过期检测

每季度对照源项目代码走一遍：

- [ ] 核心避坑还成立吗？（比如 CDN 缓存策略变了吗）
- [ ] 代码示例里的字段名 / 接口名还匹配吗？
- [ ] domain-mapping.md 的领域映射还准确吗？
- [ ] 有没有新的踩坑没沉淀？

## 待校准清单

使用者（包括自己）发现失准的地方，记录在这里。**当场修掉的也记一笔**（状态标「已修复」），保留溯源：

| 日期 | 失准点 | 提出人 | 状态 |
|------|-------|-------|------|
| 2026-08-04 | data-profiling「超大值」无可执行判据，99999 未被识别 | 羊哥实测 | 已修复（v1.2.1：加中位数×10/业务上限判据 + 画像确认前禁入聚合） |
| 2026-08-04 | excel-import 清洗「12,500」去逗号静默修正，无 warning | 羊哥实测 | 已修复（v1.2.1：格式修正也记 warning，贯彻"怎么改的用户都得知道"） |
